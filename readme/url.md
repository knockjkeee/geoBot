### 🔍 Elasticsearch: Объяснение распределения шардов  
**URL:** `http://localhost:9200/_cluster/allocation/explain?pretty`  
Показывает детальное объяснение, почему определённый шард не может быть назначен (allocated) на узел. Полезно при диагностике проблем с размещением данных в кластере Elasticsearch.

---

### 👤 Elasticsearch: Управление пользователями безопасности  
**URL:** `http://localhost:9200/_security/user?pretty`  
Возвращает список всех пользователей, настроенных в системе безопасности Elasticsearch, включая их роли и права доступа. Требует аутентификации с соответствующими привилегиями.

---

### 🩺 Elasticsearch: Состояние кластера  
**URL:** `http://localhost:9200/_cluster/health?pretty`  
Отображает общее состояние кластера Elasticsearch: статус (`green`, `yellow`, `red`), количество нод, шардов и реплик. Используется для быстрой проверки работоспособности кластера.

---

### 📊 Logstash: Статистика ноды  
**URL:** `http://localhost:5005/_node/stats?pretty`  
Предоставляет подробную статистику о работе Logstash-ноды: использование памяти, загрузка CPU, метрики обработки событий и плагинов (при условии, что включён HTTP API на порту 5005).

---

### ⚙️ Logstash: Статистика по пайплайнам  
**URL:** `http://localhost:9600/_node/stats/pipelines?pretty`  
Возвращает метрики производительности по каждому pipeline в Logstash: количество обработанных событий, ошибки, задержки и статусы. Порт 9600 — стандартный для мониторинга Logstash.

---

### 📈 Grafana: Дашборд Kafka и микросервисов  
**URL:** `http://localhost:3000/d/kafka-microservices`  
Открывает дашборд в Grafana, визуализирующий метрики Kafka и взаимодействие микросервисов: задержки, объём сообщений, лаги потребителей, ошибки и т.д.

---

### 🔎 Kibana: Обзор логов (Discover)  
**URL:** `http://localhost:5601/app/discover#`  
Интерфейс Kibana для интерактивного поиска и анализа индексированных логов и данных из Elasticsearch. Позволяет фильтровать, группировать и исследовать события в реальном времени.

---

### 📋 Logstash Monitoring API  
**URL:** `http://localhost:8090/ui/clusters/local/all-topics?perPage=25`  
Метрики производительности Logstash